<ul class="nav nav-pills nav-stacked">
    {% for advert in listAdverts %}
    <li>
        <a class="btn btn-dark" href="{{ path('ma_platform_view', {'id' : advert.id}) }}">
            {{ advert.title }}
        </a>
    </li>
    {% endfor %}
</ul>